/* eslint-disable import/no-extraneous-dependencies */
// import { html } from 'lit';
// import { expect, fixture } from '@open-wc/testing';
// import { getByRole } from '@testing-library/dom';
import './rsvp.js';

describe('Rsvp', () => {
    it('should support disabled buttons if no name', async () => {
        // const el = (await fixture(html`<lj-rsvp></lj-rsvp>`)) as HTMLElement;
        // const submitButton = getByRole(el, 'button', { name: /submit/i });
        // expect(submitButton).to.have.attribute('disabled');
    });
});
